# 🚀 ملخص الجلسة الشامل - نظام Stargate Enterprise (التحديثات واستعادة البيانات)

**تاريخ التوثيق:** 17 سبتمبر 2026  
**الإصدار المعتمد:** v2.1 (OTA & Hookah Ready)

---

## 📌 1. بيانات ورخص التفعيل الهامة:
* **رقم الجهاز (Hardware ID):** `81B2-5D8A`
* **كود التفعيل (صلاحية 10 سنوات حتى 2036):**
  ```text
  CIPQ-PUSK-4VEK-DZI7
  ```
* **رقم ورسالة الدعم الفني والصيانة المدمجة:**
  ```text
  stargate experts للصيانة التواصل على رقم 81153005
  ```

---

## 🛡️ 2. قاعدة بيانات الزبون المستعادة:
* **اسم الملف:** `stargate_production.db` (الحجم: ~335 KB).
* **المحتوى الفعلي المحفوظ:** 15 طلباً كاملاً، 13 عميلاً وزبوناً.
* **المسار الاحتياطي الدائم على الفلاشة:**
  `قاعدة_بيانات_الزبون_الأصلية\stargate_production.db`

---

## 🛠️ 3. المشاكل التي تم حلها جذرياً في هذه الجلسة:

1. **خطأ 500 في صفحة التحديثات OTA (`/admin/updates`):**
   * السبب: كان الكود القديم يطلب `url_for('login')` بدلاً من `url_for('login_page')` مما سبب انهيار الصفحة 500 عند فحص الصلاحيات، بالإضافة لنقص عمود `update_url` في جدول الإعدادات للنسخ القديمة.
   * الحل: تم تصحيح التوجيه إلى `login_page`، وإضافة آلية `ALTER TABLE` ديناميكية لإضافة أي عمود مفقود تلقائياً فور فتح الصفحة.

2. **خطأ `Method Not Allowed (405)` عند استعادة قاعدة البيانات:**
   * السبب: كان نموذج الرفع في الإعدادات يرسل البيانات عبر `POST` إلى مسار تحميل النسخة الاحتياطية الذي لم يكن يقبل سوى `GET`.
   * الحل: تم إنشاء مسار مخصص وآمن تماماً `POST /backup/restore` يقوم بفحص سلامة قاعدة البيانات المرفوعة، وأخذ نسخة احتياطية للبيانات الحالية تلقائياً قبل الاستبدال، ثم تطبيق الميجريشن وتحديث الجداول فوراً.

3. **ميزة إرجاع الأرجيلة (Hookah Return):**
   * تم تفعيل زر خاص بنقرة واحدة في جدول الطلبات: `🔥↩️` لتوثيق استرجاع الأرجيلة للمحل وتحويلها إلى `🔥✅ تم استرجاع الأرجيلة للمحل`.
   * تبويب مخصص في أعلى الجدول لفرز طلبات الأرجيلة فقط ومتابعتها.

4. **خطأ التحميل `ssl.c:1000` و `File is not a zip file`:**
   * تم تحصين `ota_updater.py` لتجاوز فحص شهادات SSL واستخدام مكتبة `requests` لتفادي أخطاء شهادات ويندوز، والتحقق من توقيع الـ ZIP السليم `PK` قبل أي فك ضغط.

---

## 📂 4. محتويات الفلاشة الحالية وكيفية التحديث للزبون:

| الملف / المجلد | الوظيفة |
|---|---|
| **`تحديث_كمبيوتر_الزبون_بضغطة_زر.bat`** | أسهل وأسرع طريقة: تحديث جهاز الزبون تلقائياً دون لمس قاعدة بياناته. |
| **`Stargate_Update_v2.1.zip`** | حزمة التحديث المضغوطة (صالحة للرفع أونلاين أو التحديث اليدوي). |
| **`StargateDelivery_NewVersion`** | النسخة الكاملة مجمعة وجاهزة للعمل ومدمج بها قاعدة البيانات. |
| **`قاعدة_بيانات_الزبون_الأصلية`** | مجلد الأمان الذي يحفظ قاعدة البيانات الأصلية للزبون دائماً. |
| **`تشغيل_البرنامج_من_الفلاشة.bat`** | لتشغيل البرنامج مباشرة من الفلاشة لأي غرض تجريبي. |

---

## 🔄 5. خطوات التحديث عن بُعد للزبائن مستقبلاً (OTA):
1. من لوحة التحكم الرئيسية (`/sg_master`): اضغط **تجميع ملف التحديث**.
2. ارفع الملف المضغوط على Google Drive وانسخ رابط التحميل المباشر.
3. في تبويب التحديثات بـ `/sg_master`: اكتب رقم الإصدار الجديد، ورابط التحميل، وملاحظات التحديث، ثم اضغط **نشر التحديث على Firebase**.
4. يفتح الزبون برنامجه ويضغط **«بحث عن تحديث الآن»** ثم **«تحميل وتثبيت التحديث الآن»** فيتحدث برنامجه تلقائياً في ثوانٍ.
