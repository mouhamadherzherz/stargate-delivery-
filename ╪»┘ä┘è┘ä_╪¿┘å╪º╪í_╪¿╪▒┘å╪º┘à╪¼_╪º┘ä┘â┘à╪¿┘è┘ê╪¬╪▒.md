# Stargate Delivery — برنامج الكمبيوتر

## النتيجة

يعمل النظام كبرنامج Windows مستقل داخل نافذة خاصة به. يشغّل البرنامج خادم Flask محلياً على جهاز المستخدم، ثم يفتح الواجهة داخل نافذة PyWebView. لا يحتاج المستخدم إلى فتح Chrome أو Edge يدوياً.

## البيانات

تحفظ قاعدة البيانات والنسخ الاحتياطية في مجلد `data` بجانب البرنامج. لا تضع قاعدة البيانات داخل ملف EXE، لأن تحديث البرنامج يجب ألا يحذف بيانات المستخدم.

## البناء على Windows

1. ثبّت Python 3.11 أو أحدث، ثم افتح PowerShell داخل مجلد المشروع.
2. شغّل `build_desktop.bat` من جهاز Windows.
3. سيُنشأ الملف `dist\\StargateDelivery.exe`.
4. شغّل البرنامج أو استخدم ملف Inno Setup لإنشاء مثبت.

يجب تنفيذ البناء على Windows لأن PyWebView يحتاج محرك WebView2 أو EdgeChromium المتوفر في بيئة Windows. في أول تشغيل، ينشئ التطبيق مفاتيح الجلسة ومجلد البيانات تلقائياً.

## تشغيل التطوير

```powershell
python app.py --desktop
```

إذا لم تتوفر PyWebView، يفتح التطبيق الرابط المحلي في المتصفح كحل احتياطي، ولا تتأثر وظائف Flask.

## التثبيت والتحديث

- يحتفظ المثبت بمجلد `data` عند التحديث.
- خذ نسخة احتياطية قبل تحديث كبير.
- لا تنسخ ملف `.env` أو أي مفاتيح سرية إلى مستودع أو ملف توزيع عام.
- غيّر بيانات المدير أثناء أول تشغيل؛ لا توجد بيانات اعتماد افتراضية ثابتة في النسخة المنقحة.

## ملاحظات تشغيلية

البرنامج مهيأ للعمل المحلي على `127.0.0.1:8085`، ويتجنب المنفذين 5000 و5001 المحجوزين لتطبيقات أخرى. إذا كان المنفذ مشغولاً، استخدم متغير البيئة `PORT` في تشغيل الخادم التقليدي أو أغلق العملية القديمة قبل بدء البرنامج المكتبي.
